'use strict';
	function gjk3d(nodes1, nodes2) {
		//this goes outside this function:
		//var nodes1 = o1.nodes * o1.state
		//var nodes2 = o2.nodes * o2.state
		
		/*
		while(true) {
			switch(simplex.length) {
				case 0: 
					d1=c2-c1;
					p1=max(nodes1,d1) - min(nodes2,-d1);
				case 1:
					d2=-d1;
					p2=max(nodes1,d2) - min(nodes2,-d2);
				case 2:
					d3=(p2p1 x p1) x p2p1;
					p3=max(nodes1,d3) - min(nodes2,-d3);
				case 3:
					d4=(p2p1 x p3p1);
					d4 * p3 > 0 ? d4=-d4
					p4=max(nodes1,d4) - min(nodes2,d4);
				case 4:
					
			}
		}s
		*/
	}
	
	function min() {}
	function max() {}